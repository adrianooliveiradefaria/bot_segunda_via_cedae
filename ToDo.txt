- Tratar parâmetro --config_pk para gerar chave criptográfica privada
    * python agua.py --config_pk
- Gerar chave criptográfica privada
    * Instanciar Criptografia().gerar_chave_cripto()
- Tratar parâmetro --config-smtp para configurar parâmetros do servidor smtp
    * host [str]
    * porta [int] SSL
    * usuário [str]
    * senha [str]
- Criptografar chave informada para a senha da conta de envio de e-mail
    * Instanciar Criptografia().encriptar(chave [str])
- Gravar a chave criptografada como valor da chave senha no arquivo config/smtp.yaml
    * Instanciar ArquivoConfig(smtp.yaml).gravar_arquivo(conteudo [dict])
